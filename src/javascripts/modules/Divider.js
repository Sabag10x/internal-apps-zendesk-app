import * as React from "react";

export const Divider = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "1px",
        backgroundColor: "lightgray",
        borderRadius: "8px",
        margin: "1em 0",
      }}
    />
  );
};
